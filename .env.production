# Production build env — read automatically by `vite build`.
# NOTE: VITE_* vars are inlined into the client bundle, so anything here is
# publicly visible to the browser. Do not put real secrets here.
VITE_API_BASE_URL=https://pravai-api.ai-bek.com
